import { ImpressivePipe } from './impressive.pipe';

describe('ImpressivePipe', () => {
  it('create an instance', () => {
    const pipe = new ImpressivePipe();
    expect(pipe).toBeTruthy();
  });
});
